- facts.group_by(&:activity_id).each do |activity_id, facts|
  %div
    %span{title: activity_id}= facts.first.activity.name
    = render_minutes(facts.inject(0) {|sum, f| sum + f.duration_in_minutes }).join(':')